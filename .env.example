# ============================================================
# PostgreSQL — dùng khi chạy local (Docker / bản gốc)
# ============================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smartlibrary
DB_USER=postgres
DB_PASSWORD=your_password_here

# ============================================================
# Railway / Render — tự động gán (DATABASE_URL do platform cung cấp)
# Khi có DATABASE_URL, ưu tiên dùng nó thay vì các biến trên
# ============================================================
# DATABASE_URL=postgres://user:password@host:5432/db

# ============================================================
# Gemini AI
# ============================================================
GEMINI_API_KEY=AIzaSyAAlHjje9tmoDIGkoxMfrrWmwzuYtKh3LA

# ============================================================
# Email SMTP (optional — không cần cho demo/thuyết trình)
# ============================================================
# MAIL_SMTP_HOST=smtp.gmail.com
# MAIL_SMTP_PORT=587
# MAIL_SMTP_USER=your_email@gmail.com
# MAIL_SMTP_PASSWORD=your_app_password_here
# MAIL_FROM=your_email@gmail.com
